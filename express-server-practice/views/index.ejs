<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <h1>Working!</h1>
    <% if(todos.length > 0) { %>
        <h2>Todos!!</h2>
        <% } else { %>
        <h2>No Todos to show.</h2>
        <% } %> 
    
        <% if(todos) { %>
        <ul>
            <% todos.forEach(todo => { %>
            <li><%=todo.title%> | <%=todo.category%></li>
            <% }) %>
        </ul>
        <% } %>

    <%- include('./partials/footer'); %>
</body>

</html>